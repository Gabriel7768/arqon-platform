# ============================================================================
# ARQON SaaS — .gitignore (comprehensive, SaaS-grade)
# ============================================================================
# Stack: Node.js 24, TypeScript 5.9, pnpm workspaces, React 19 + Vite,
#        Express 5, PostgreSQL + Drizzle ORM, Zod, JWT, Render, Vercel,
#        Supabase, Abacatepay.
# Purpose: keep secrets, build artifacts, OS/IDE noise, and local state
#          out of version control. Defense-in-depth — even if a secret
#          is accidentally placed in a tracked location, these patterns
#          provide a backstop.
# ============================================================================


# ---------------------------------------------------------------------------
# SECRETS & CREDENTIALS  (NEVER commit these — highest priority)
# ---------------------------------------------------------------------------
# Environment files (the #1 vector for leaked credentials)
.env
.env.*
!.env.example
.env.local
.env.development
.env.production
.env.staging
.env.test
.env.*.local

# API keys / tokens / passwords stored as flat files
*.pem
*.key
*.crt
*.cert
*.p12
*.pfx
*.keystore
*.jks
*.pub
!**/public/**/*.pub

# Secret manager / vault files
secrets/
.secrets/
credentials/
creds/
*.secret
*.token

# Cloud provider credentials
.aws/
.aws/
.gcp/
.azure/
CLI/
.fly/
.render/
.vercel/.env
.vercel/.env.local

# Supabase
supabase/.env
supabase/.branches
supabase/.temp

# SSH (never commit private keys)
~/.ssh/
*.ssh
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
known_hosts
authorized_keys


# ---------------------------------------------------------------------------
# DEPENDENCIES  (regenerated from lockfile — never commit)
# ---------------------------------------------------------------------------
node_modules/
.pnpm-store/
.yarn/
.yarn/cache/
.yarn/install-state.gz
.yarn/build-state.yml
jspm_packages/
bower_components/

# pnpm: the LOCKFILE IS committed (pnpm-lock.yaml). Do NOT ignore it.
# Only ignore stale non-pnpm lockfiles if they sneak in:
package-lock.json
yarn.lock
bun.lockb


# ---------------------------------------------------------------------------
# BUILD OUTPUT  (regenerated from source)
# ---------------------------------------------------------------------------
# Compiled / bundled
dist/
build/
out/
out-tsc/
*.tsbuildinfo
.tsbuildinfo

# Vite / Rollup
.vite/
.rollup.cache/

# TypeScript
*.js.map
*.mjs.map
*.d.ts.map
# (keep source maps out of VCS; they can be generated on build)

# esbuild
.esbuild/

# Next.js (if ever used)
.next/
.vercel/cache/

# Expo / React Native (if ever used)
.expo/
.expo-shared/
.expo/dist/

# Storybook
storybook-static/


# ---------------------------------------------------------------------------
# TESTING & COVERAGE  (regenerated by test runs)
# ---------------------------------------------------------------------------
coverage/
.nyc_output/
*.lcov
.jest-cache/
.vitest-cache/
test-results/
playwright-report/
playwright/.cache/
testem.log
xunit.xml
junit.xml


# ---------------------------------------------------------------------------
# LOGS & RUNTIME STATE
# ---------------------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-debug.log
connect.lock
libpeerconnection.log


# ---------------------------------------------------------------------------
# LOCAL DATABASES & DATA FILES  (never ship DB snapshots)
# ---------------------------------------------------------------------------
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.sqlar
*.dump
*.backup
*.bak
dumps/
db-dumps/
*.sql.bak

# Postgres local
pgdata/
postgres-data/
*.pgsql


# ---------------------------------------------------------------------------
# UPLOADS & USER-GENERATED CONTENT  (SaaS handles CSV uploads — never commit)
# ---------------------------------------------------------------------------
uploads/
uploads/*
!uploads/.gitkeep
!uploads/README.md
*.csv
!**/sample*.csv
!**/example*.csv
!**/fixtures/**/*.csv
!**/__tests__/**/*.csv
*.xlsx
*.xls
*.parquet
*.jsonl
# (allow committed demo/fixture CSVs only via explicit negations above)


# ---------------------------------------------------------------------------
# CACHE & TEMPORARY FILES
# ---------------------------------------------------------------------------
.cache/
.tmp/
tmp/
.temp/
*.tmp
*.temp
.eslintcache
.stylelintcache
.parcel-cache/
.fusebox/
.turbo/
.turbo.json.swp


# ---------------------------------------------------------------------------
# EDITORS & IDES  (developer-specific — never commit)
# ---------------------------------------------------------------------------
# VS Code (keep shared configs, ignore personal state)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets
.vscode/*.log
.vscode/.ropeproject/
.history/

# JetBrains (IntelliJ, WebStorm, etc.)
.idea/
.idea/*
*.iml
*.ipr
*.iws
.idea_modules/

# Sublime
*.sublime-workspace
*.sublime-project
.sublime-github/

# Vim
*.swp
*.swo
*~
.netrwhist
Session.vim
.vim/

# Emacs
*~
\#*\#
/.emacs.desktop
.elc
auto-save-list
tramp

# Cursor / Continue / Aider
.cursor/
.cursorignore
.cursorrules
.continue/
.aider*
.aiderignore


# ---------------------------------------------------------------------------
# OPERATING SYSTEM NOISE
# ---------------------------------------------------------------------------
# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Icon?

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/

# Linux
*~
.directory
.Trash-*

# Mobile
.DS_Store


# ---------------------------------------------------------------------------
# CI / CD ARTIFACTS  (regenerated by pipelines)
# ---------------------------------------------------------------------------
.cache/
.github/cache/
.artifacts/
deployment-artifacts/
*.tar
*.tar.gz
*.zip


# ---------------------------------------------------------------------------
# MONOREPO-SPECIFIC  (ARQON layout)
# ---------------------------------------------------------------------------
# Replit
.replit
.replitignore
.cache/
.local/

# pnpm workspace — keep pnpm-workspace.yaml and pnpm-lock.yaml (committed!)
# but ignore per-package lock noise
**/pnpm-lock.yaml.bak

# Generated API client (regenerated from OpenAPI via Orval codegen)
# NOTE: @workspace/api-client-react/src/generated and @workspace/api-zod/src/generated
# are intentionally COMMITTED in this repo (codegen output checked in for auditability).
# If your team prefers regenerating at build time, uncomment:
# lib/api-client-react/src/generated/
# lib/api-zod/src/generated/

# AI / agent working files (local only)
.agents/
.agent_tmp/
memory/
prompts/
.local/skills/


# ---------------------------------------------------------------------------
# DOCUMENTATION BUILD  (if you generate docs site)
# ---------------------------------------------------------------------------
_site/
.docusaurus/
.cache-loader/
.jekyll-metadata
_book/
docs/_build/


# ---------------------------------------------------------------------------
# DOCKER  (keep Dockerfile + compose; ignore runtime volumes)
# ---------------------------------------------------------------------------
.docker/
docker-compose.override.yml
*.pid
*.seed


# ---------------------------------------------------------------------------
# MISC
# ---------------------------------------------------------------------------
.gh-token
.github-token
*.bak
*.orig
*.rej
*.patch
*.diff
crash.log
crash-dump.log
node.error.log

# Lock files that are NOT package managers
*.lock
!pnpm-lock.yaml
!uv.lock
!bun.lock

# End of .gitignore
